import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const CountryList = () => {
  return (
    <View>
      <Text> Country List</Text>
    </View>
  );
};

export default CountryList;
