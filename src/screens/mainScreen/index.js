import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const MainScreen = () => {
  return (
    <View>
      <Text> Main Screen</Text>
    </View>
  );
};

export default MainScreen;
